#!/bin/bash
## Improt aliases

mkdir -p ~/docker-registry
mkdir -p ~/docker-temp/{postgres,mongodb,minio,redis,valhalla,traccar-data,traccar-logs}
# rm -r -f -I ~/docker-temp/{postgres,mongodb,minio,redis,valhalla,traccar-data,traccar-logs}/.
